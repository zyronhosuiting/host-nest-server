services:
  postgres:
    image: postgres:16-alpine
    container_name: host-living-db
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: host_living
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
